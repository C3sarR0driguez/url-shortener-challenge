import { createStyles } from "@material-ui/core/styles";

const styles = _ =>
  createStyles({
    root: {
      color: "#fff"
    },
    jumbo: {
      fontSize: 80
    },
    big: {
      fontSize: 50
    },
    medium: {
      fontSize: 25
    },
    small: {
      fontSize: 10
    }
  });
export default styles;
